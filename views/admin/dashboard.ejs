<div class="admin-controls">
  <div class="btn-container">
    <a class="btn-admin" id="reg-admin-btn">
      <h3>New Admin</h3>
    </a>
    <a class="btn-admin" id="add-qs-btn">
      <h3>Add Question</h3>
    </a>
    <a href="/student-list" class="btn-admin">
      <h3>Manage Tutions</h3>
    </a>
    <a href="/reg-list" class="btn-admin">
      <h3>Manage Tutor Registration</h3>
    </a>
  </div>

  <form id="registerForm" action="/register" method="POST" class="reg-admin-form">
    <label for="username"><b>Username</b></label>
    <input type="text" placeholder="Enter Username" name="username">
    <label for="password"><b>Password</b></label>
    <input type="password" placeholder="Enter Password" name="password">
    <input type="submit" value="Register" class="btn">
  </form>

  <form id="addQuestionForm" action="/add-question" method="POST" class="add-question-form" style="display: none;">
    <label for="questionText"><b>Question Text</b></label>
    <textarea placeholder="Enter the question text" name="questionText" required></textarea>

    <label for="category"><b>Category</b></label>
    <select name="category" required>
      <option value="math">Math</option>
      <option value="science">Science</option>
      <option value="history">History</option>
      <option value="bangla">Bangla</option>
      <option value="english">English</option>
      <option value="religion">Religion</option>
      <option value="IQ">IQ</option>
      <option value="other">Other</option>
    </select>

    <input type="submit" value="Add Question" class="btn">
  </form>
</div>

<div class="admin-tutorsdiv">
  <h2>Tutors</h2>
  <a href="/add-tutor" class="btn-add">+ Add New</a>

  <table class="admin-tutors">
    <thead>
      <tr>
        <th>Name</th>
        <th>ID</th>
        <th>Tag</th>
        <th>Cont</th>
        <th>Inst</th>
        <th>Dept</th>
        <th>HSC</th>
        <th>Back</th>
        <th>College</th>
        <th>Exp</th>
        <th>Mode</th>
        <th>Pref</th>
        <th>Rate</th>
        <th>Action</th>
      </tr>
    </thead>
    <tbody>
      <% data.forEach(tutor=> { %>
        <tr>
          <td>
            <%= tutor.name %>
          </td>
          <td>
            t<%= tutor.id %>
          </td>
          <td>
            <%= tutor.tag %>
          </td>
          <td>
            <a href="<%= tutor.fb %>" class="btn-icon">ⓕ</a>
            <a href="mailto:<%= tutor.email %>" style="text-decoration: none;">📧</a>
            <a href="tel:<%= tutor.phone %>" class="btn-icon">📞</a>
          </td>
          <td>
            <%= tutor.institution %>
          </td>
          <td>
            <%= tutor.dept %>
          </td>
          <td>
            <%= tutor.hsc %>
          </td>
          <td>
            <%= tutor.background %>
          </td>
          <td>
            <%= tutor.college %>
          </td>
          <td>
            <%= tutor.expertise %>
          </td>
          <td>
            <%= tutor.mode %>
          </td>
          <td>
            <%= tutor.pref %>
          </td>
          <td>
            <%= tutor.rating %>
          </td>
          <td>
            <div style="display: flex; gap: 5px;">
              <a href="/inc_rating/<%= tutor._id %>" class="btn-icon">⬆️</a>
              <a href="/dec_rating/<%= tutor._id %>" class="btn-icon">⬇️</a>
            </div>
            <form action="/delete-tutor/<%= tutor._id %>?_method=DELETE" method="POST">
              <input type="submit" value="Del" class="btn-delete btn">
            </form>
          </td>
        </tr>
        <% }) %>
    </tbody>
  </table>
</div>
